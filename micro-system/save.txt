import urequests
import network
import time
import esp
import gc

gc.collect()
esp.osdebug(None)

ssid = 'MaZahaka :)'

password = '19926818'


station = network.WLAN(network.STA_IF)
station.active(True)
station.connect(ssid, password)

print(sta_if.isconnected())
while station.isconnected() == False:
    pass
print('Connection successful')

print(station.ifconfig())

request = urequests.get('https://api.openweathermap.org/data/2.5/weather')
